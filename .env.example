# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Database URL (Railway provides this automatically in production)
# For local development:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pethospital_kpi

# Port (Railway provides $PORT automatically)
PORT=8000

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================
# CORS - Allowed origins (comma-separated)
# For production, restrict to specific domains
# Example: https://codex.example.com,https://admin.example.com
ALLOWED_ORIGINS=*

# Dashboard Authentication
# Set strong credentials for production
DASHBOARD_USERNAME=admin
DASHBOARD_PASSWORD=change-this-secure-password

# =============================================================================
# RATE LIMITING
# =============================================================================
# Rate limits per endpoint (requests per time window)
# Format: number/unit (e.g., 100/day, 60/minute)
RATE_LIMIT_SUBMIT=100/day
RATE_LIMIT_EVENTS=1000/day
RATE_LIMIT_DASHBOARD=60/minute

# =============================================================================
# MONITORING & LOGGING
# =============================================================================
# Sentry DSN for error tracking (optional)
# Get from https://sentry.io
SENTRY_DSN=

# Environment name (development, staging, production)
ENVIRONMENT=development

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
# Application title (shown in API docs and dashboard)
APP_TITLE=PetHospital KPI Service

# Application version
APP_VERSION=2.0.0

# Enable API documentation (set to false in production for security)
ENABLE_DOCS=true
